export function SlidersDemo(props: { onClose: () => void }) {
  return (
    <Window title="Sliders & Drags Demo" open={true} onClose={props.onClose}>
      <Column gap={8}>
        <Text>Sliders & Drags demo — coming soon.</Text>
      </Column>
    </Window>
  );
}
