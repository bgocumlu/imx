export function InputsDemo(props: { onClose: () => void }) {
  return (
    <Window title="Inputs Demo" open={true} onClose={props.onClose}>
      <Column gap={8}>
        <Text>Inputs demo — coming soon.</Text>
      </Column>
    </Window>
  );
}
