export function MenusDemo(props: { onClose: () => void }) {
  return (
    <Window title="Menus & Popups Demo" open={true} onClose={props.onClose}>
      <Column gap={8}>
        <Text>Menus & Popups demo — coming soon.</Text>
      </Column>
    </Window>
  );
}
